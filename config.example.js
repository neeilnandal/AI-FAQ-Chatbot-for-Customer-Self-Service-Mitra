/*
  Example-only configuration file.

  Use this as a safe template for documenting the public chatbot settings.
  Do not commit private credentials or admin tokens.
*/

const mitraBotpressConfig = {
  composerPlaceholder: "Chat with Mitra",
  botConversationDescription: "Customer support chatbot for FAQ self-service",
  botName: "Mitra",
  themeName: "prism",
  themeColor: "#2563eb",
  useSessionStorage: true,
  showBotInfoPage: true,
  allowedOrigins: ["https://example.com"]
};

export default mitraBotpressConfig;
